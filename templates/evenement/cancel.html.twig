{% extends 'base.html.twig' %}

{% block title %}Annuler l'événement — {{ evenement.titre }}{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="card border-warning shadow-sm">
                <div class="card-header bg-light d-flex align-items-center gap-2">
                    <i class="fas fa-ban text-warning"></i>
                    <strong>Annuler l'événement</strong>
                </div>
                <div class="card-body">
                    <p class="text-muted mb-3">
                        L'événement <strong>{{ evenement.titre }}</strong> a {{ evenement.reservations|length }} réservation(s). En annulant, les participants seront notifiés par email avec le motif que vous indiquez et vos coordonnées pour le remboursement si l'événement est payant.
                    </p>
                    <form method="post" action="{{ path('app_evenement_cancel', {id: evenement.id}) }}">
                        <input type="hidden" name="_token" value="{{ csrf_token('cancel' ~ evenement.id) }}">
                        <div class="mb-3">
                            <label for="motif_annulation" class="form-label">Motif d'annulation <span class="text-danger">*</span></label>
                            <textarea name="motif_annulation" id="motif_annulation" class="form-control" rows="4" required placeholder="Indiquez la raison de l'annulation (visible dans l'email envoyé aux participants).">{{ app.request.request.get('motif_annulation') }}</textarea>
                        </div>
                        <div class="d-flex gap-2 justify-content-end">
                            <a href="{{ path('app_evenement_show', {id: evenement.id}) }}" class="btn btn-outline-secondary">Retour</a>
                            <button type="submit" class="btn btn-warning">
                                <i class="fas fa-ban me-1"></i> Annuler l'événement
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
