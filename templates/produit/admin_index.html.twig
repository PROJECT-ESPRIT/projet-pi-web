{% extends 'base.html.twig' %}

{% block title %}Gestion des Produits{% endblock %}

{% block body %}
<<<<<<< HEAD
<div class="container mt-4">

    <h1 class="mb-4">Gestion des Produits (Analyse IA)</h1>

    <div class="mb-3 text-end">
        <a href="{{ path('app_produit_new') }}" class="btn btn-success">
            Ajouter un produit
        </a>
    </div>

    <table class="table table-striped table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prix</th>
                <th>Stock</th>
                <th>Prévision (mois prochain)</th>
                <th>Risque Rupture</th>
                <th>Graphique</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.id }}</td>
                <td>{{ produit.nom }}</td>
                <td>{{ produit.prix }} TND</td>
                <td>
                    {{ produit.stock }}

                    {% if predictions[produit.id].rupture %}
                        <span class="badge bg-danger ms-2">⚠ Stock insuffisant</span>
                    {% endif %}
                </td>

                <td class="fw-bold text-primary">
                    {{ predictions[produit.id].prediction }}
                </td>

                <td>
                    {% if predictions[produit.id].rupture %}
                        <span class="badge bg-danger">Risque élevé</span>
                    {% else %}
                        <span class="badge bg-success">Stable</span>
                    {% endif %}
                </td>

                <td>
                    <canvas id="chart{{ produit.id }}" width="180" height="90"></canvas>
                </td>

                <td>
                    <a href="{{ path('app_produit_edit', {'id': produit.id}) }}" 
                       class="btn btn-sm btn-warning">
                        Éditer
                    </a>

                    {{ include('produit/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8" class="text-center">
                    Aucun produit trouvé
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}

{% block javascripts %}
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
{% for produit in produits %}
    new Chart(
        document.getElementById('chart{{ produit.id }}'),
        {
            type: 'line',
            data: {
                labels: ['M-3','M-2','M-1','M'],
                datasets: [{
                    label: 'Ventes',
                    data: {{ predictions[produit.id].historique|json_encode|raw }},
                    borderColor: 'blue',
                    backgroundColor: 'rgba(0, 123, 255, 0.1)',
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: false,
                plugins: {
                    legend: { display: false }
                },
                scales: {
                    y: { beginAtZero: true }
                }
            }
        }
    );
{% endfor %}
</script>
=======
    <div class="row align-items-center mb-3">
        <div class="col">
            <h1>Gestion des Produits</h1>
        </div>
        <div class="col-auto">
            <a href="{{ path('app_produit_new') }}" class="btn btn-success">Ajouter un produit</a>
        </div>
    </div>

    <div class="card mb-3">
        <div class="card-body">
            <form method="get" action="{{ path('app_produit_admin_index') }}" class="row g-2 align-items-end">
                <div class="col-12 col-md-4">
                    <label class="form-label">Recherche</label>
                    <input type="text" name="q" class="form-control" placeholder="Rechercher un produit..." value="{{ search|default('') }}">
                </div>
                <div class="col-6 col-md-2">
                    <label class="form-label">Trier par</label>
                    <select name="sort" class="form-select">
                        <option value="id" {{ (sort|default('id')) == 'id' ? 'selected' : '' }}>ID</option>
                        <option value="nom" {{ (sort|default('id')) == 'nom' ? 'selected' : '' }}>Nom</option>
                        <option value="prix" {{ (sort|default('id')) == 'prix' ? 'selected' : '' }}>Prix</option>
                        <option value="stock" {{ (sort|default('id')) == 'stock' ? 'selected' : '' }}>Stock</option>
                    </select>
                </div>
                <div class="col-6 col-md-2">
                    <label class="form-label">Ordre</label>
                    <select name="direction" class="form-select">
                        <option value="asc" {{ (direction|default('asc')) == 'asc' ? 'selected' : '' }}>Ascendant</option>
                        <option value="desc" {{ (direction|default('asc')) == 'desc' ? 'selected' : '' }}>Descendant</option>
                    </select>
                </div>
                <div class="col-12 col-md-4 d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Filtrer</button>
                    <a href="{{ path('app_produit_admin_index') }}" class="btn btn-outline-secondary">Reset</a>
                </div>
            </form>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Prix</th>
                        <th>Stock</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for produit in produits %}
                    <tr>
                        <td>{{ produit.id }}</td>
                        <td>{{ produit.nom }}</td>
                        <td>{{ produit.prix }} TND</td>
                        <td>{{ produit.stock }}</td>
                        <td>
                            <div class="d-flex flex-wrap gap-1">
                                <a href="{{ path('app_produit_edit', {'id': produit.id}) }}" class="btn btn-sm btn-outline-primary">Editer</a>
                                {{ include('produit/_delete_form.html.twig') }}
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">Aucun produit trouve</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
>>>>>>> 0fa83a3c8c69b9b4980567bb2faa12289b7c6a4b
{% endblock %}
