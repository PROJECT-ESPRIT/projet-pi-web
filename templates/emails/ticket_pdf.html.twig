<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body { font-family: Helvetica, Arial, sans-serif; background: #f0eef9; padding: 20px; }
        .ticket {
            background: #fff;
            border-radius: 16px;
            overflow: hidden;
            max-width: 700px;
            margin: 0 auto;
            border: 2px solid #e0ddf0;
        }
        .ticket-header {
            background: linear-gradient(135deg, #1e0f75 0%, #3785d8 60%, #bf8ce1 100%);
            color: #fff;
            padding: 24px 30px 20px;
            text-align: center;
        }
        .ticket-header h1 {
            font-size: 22px;
            font-weight: 800;
            margin: 0 0 4px;
            letter-spacing: -0.02em;
        }
        .ticket-header p {
            font-size: 12px;
            opacity: 0.85;
            margin: 0;
        }
        .ticket-badge {
            display: inline-block;
            background: rgba(255,255,255,0.2);
            border: 1px solid rgba(255,255,255,0.3);
            border-radius: 50px;
            padding: 4px 14px;
            font-size: 10px;
            font-weight: 700;
            letter-spacing: 0.06em;
            margin-bottom: 10px;
        }
        .ticket-body {
            display: table;
            width: 100%;
        }
        .ticket-left {
            display: table-cell;
            width: 65%;
            padding: 24px 28px;
            vertical-align: top;
        }
        .ticket-right {
            display: table-cell;
            width: 35%;
            padding: 24px 20px;
            vertical-align: top;
            text-align: center;
            border-left: 2px dashed #e0ddf0;
            background: #fafbff;
        }
        .ticket-event-name {
            font-size: 18px;
            font-weight: 800;
            color: #1e0f75;
            margin-bottom: 14px;
            line-height: 1.25;
        }
        .ticket-row {
            margin-bottom: 10px;
        }
        .ticket-label {
            font-size: 9px;
            font-weight: 700;
            color: #9ca3af;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            margin-bottom: 2px;
        }
        .ticket-value {
            font-size: 13px;
            font-weight: 600;
            color: #1f2937;
        }
        .ticket-value.highlight {
            color: #1e0f75;
            font-weight: 800;
        }
        .ticket-value.green {
            color: #059669;
            font-weight: 800;
        }
        .ticket-grid {
            display: table;
            width: 100%;
        }
        .ticket-grid-cell {
            display: table-cell;
            width: 50%;
            vertical-align: top;
        }
        .ticket-qr-label {
            font-size: 9px;
            font-weight: 700;
            color: #6b7280;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            margin-top: 10px;
        }
        .ticket-id {
            font-size: 11px;
            color: #9ca3af;
            margin-top: 6px;
        }
        .ticket-status {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 8px;
            font-size: 10px;
            font-weight: 700;
            margin-top: 8px;
        }
        .status-confirmed { background: #d1fae5; color: #059669; }
        .status-pending { background: #fef3c7; color: #b45309; }
        .status-cancelled { background: #fee2e2; color: #dc2626; }
        .ticket-footer {
            background: #f8f9fc;
            border-top: 1px solid #e0ddf0;
            padding: 12px 28px;
            text-align: center;
            font-size: 10px;
            color: #9ca3af;
        }
        .ticket-event-banner {
            width: 100%;
            overflow: hidden;
            margin: 0;
            padding: 0;
        }
        .ticket-event-img {
            width: 100%;
            height: 120px;
            object-fit: cover;
            display: block;
            border: none;
            margin: 0;
        }
        .ticket-divider {
            border: none;
            border-top: 1px dashed #e0ddf0;
            margin: 12px 0;
        }
    </style>
</head>
<body>
    <div class="ticket">
        <div class="ticket-header">
            <div class="ticket-badge">BILLET D'ENTRÉE</div>
            <h1>Art Connect</h1>
            <p>Votre ticket de réservation</p>
        </div>

        {% if event_image_data_uri %}
        <div class="ticket-event-banner">
            <img src="{{ event_image_data_uri }}" alt="{{ evenement.titre }}" class="ticket-event-img">
        </div>
        {% endif %}

        <div class="ticket-body">
            <div class="ticket-left">
                <div class="ticket-event-name">{{ evenement.titre }}</div>

                <div class="ticket-grid">
                    <div class="ticket-grid-cell">
                        <div class="ticket-row">
                            <div class="ticket-label">Participant</div>
                            <div class="ticket-value highlight">{{ participant_name }}</div>
                        </div>
                    </div>
                    <div class="ticket-grid-cell">
                        <div class="ticket-row">
                            <div class="ticket-label">Statut</div>
                            <div>
                                {% if reservation.status == 'CONFIRMED' %}
                                    <span class="ticket-status status-confirmed">Confirmé</span>
                                {% elseif reservation.status == 'PENDING' %}
                                    <span class="ticket-status status-pending">En attente</span>
                                {% else %}
                                    <span class="ticket-status status-cancelled">Annulé</span>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>

                <hr class="ticket-divider">

                <div class="ticket-grid">
                    <div class="ticket-grid-cell">
                        <div class="ticket-row">
                            <div class="ticket-label">Date début</div>
                            <div class="ticket-value">{{ event_date }}</div>
                        </div>
                    </div>
                    <div class="ticket-grid-cell">
                        <div class="ticket-row">
                            <div class="ticket-label">Date fin</div>
                            <div class="ticket-value">{{ event_date_end ?: '—' }}</div>
                        </div>
                    </div>
                </div>

                <div class="ticket-grid">
                    <div class="ticket-grid-cell">
                        <div class="ticket-row">
                            <div class="ticket-label">Lieu</div>
                            <div class="ticket-value">{{ lieu }}</div>
                        </div>
                    </div>
                    <div class="ticket-grid-cell">
                        <div class="ticket-row">
                            <div class="ticket-label">{% if seat_label %}Place{% else %}Prix{% endif %}</div>
                            <div class="ticket-value {% if seat_label %}highlight{% else %}green{% endif %}">
                                {{ seat_label ?: prix }}
                            </div>
                        </div>
                    </div>
                </div>

                {% if seat_label %}
                <div class="ticket-row">
                    <div class="ticket-label">Prix</div>
                    <div class="ticket-value green">{{ prix }}</div>
                </div>
                {% endif %}

                <div class="ticket-row" style="margin-top:8px;">
                    <div class="ticket-label">Réservé le</div>
                    <div class="ticket-value">{{ reservation_date }}</div>
                </div>
            </div>

            <div class="ticket-right">
                <img src="{{ qr_code }}" alt="QR Code" style="width:160px;height:160px;border-radius:10px;border:2px solid #e0ddf0;">
                <div class="ticket-qr-label">Scannez pour vérifier</div>
                <div class="ticket-id">Réservation #{{ reservation.id }}</div>
            </div>
        </div>

        <div class="ticket-footer">
            Art Connect — L'art inclusif pour tous &middot; &copy; {{ 'now'|date('Y') }} &middot; Ce billet est personnel et non transférable.
        </div>
    </div>
</body>
</html>
