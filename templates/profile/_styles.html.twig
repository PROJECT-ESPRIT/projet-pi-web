<style>
    .fo-profile-shell { display: flex; flex-direction: column; gap: .9rem; }
    .fo-profile-header { display: flex; justify-content: space-between; align-items: center; gap: .8rem; flex-wrap: wrap; }
    .fo-profile-title { font-family: 'Sora', sans-serif; font-weight: 700; font-size: 1.45rem; color: var(--ac-deep); margin: 0; display: flex; align-items: center; gap: .55rem; }
    .fo-profile-subtitle { margin: 0; font-size: .88rem; color: #6b7280; }

    .fo-profile-card {
        background: linear-gradient(160deg, rgba(255,255,255,1) 0%, rgba(173,198,229,.14) 100%);
        border: 1px solid #e9ecef;
        border-radius: 14px;
        box-shadow: 0 3px 12px rgba(30, 15, 117, .07);
        overflow: hidden;
    }
    .fo-profile-card-body { padding: 1rem; }
    .fo-profile-top { display: flex; justify-content: space-between; align-items: center; gap: .9rem; flex-wrap: wrap; margin-bottom: .8rem; }
    .fo-profile-identity { display: flex; align-items: center; gap: .8rem; }
    .fo-profile-avatar {
        width: 82px; height: 82px; border-radius: 50%; overflow: hidden;
        display: flex; align-items: center; justify-content: center;
        color: #fff; font-weight: 700; font-size: 1.25rem;
        background: linear-gradient(135deg, var(--ac-mid), var(--ac-blue));
        border: 2px solid rgba(255,255,255,.75);
        box-shadow: 0 6px 16px rgba(28,29,171,.2);
    }
    .fo-profile-avatar img { width: 100%; height: 100%; object-fit: cover; display: block; }
    .fo-profile-role {
        display: inline-flex;
        border-radius: 999px;
        padding: .2rem .58rem;
        font-size: .67rem;
        font-weight: 700;
        letter-spacing: .04em;
        text-transform: uppercase;
        color: var(--ac-mid);
        background: rgba(191,140,225,.16);
        border: 1px solid rgba(191,140,225,.32);
    }
    .fo-profile-name { margin: .34rem 0 .15rem; font-size: 1.14rem; font-weight: 800; color: #111827; }
    .fo-profile-age { margin: 0; font-size: .84rem; color: #6b7280; }

    .fo-profile-grid { display: grid; gap: .48rem; grid-template-columns: 1fr; margin-top: .2rem; }
    .fo-profile-meta {
        border-radius: 9px;
        border: 1px solid rgba(55,133,216,.12);
        background: rgba(255,255,255,.78);
        padding: .5rem .58rem;
    }
    .fo-profile-meta .label { margin: 0; color: #6b7280; font-size: .74rem; font-weight: 600; }
    .fo-profile-meta .value { margin: .08rem 0 0; color: #1f2937; font-size: .86rem; font-weight: 700; }

    .fo-profile-note {
        margin-top: .62rem;
        border-radius: 10px;
        border: 1px solid rgba(30,15,117,.12);
        background: rgba(173,198,229,.2);
        padding: .52rem .65rem;
        font-size: .8rem;
        color: #334155;
    }
    .fo-profile-note i { color: var(--ac-mid); }

    .fo-profile-actions { display: flex; flex-wrap: wrap; gap: .45rem; }
    .fo-profile-actions .btn { border-radius: 10px; font-weight: 700; font-size: .8rem; }
    .fo-profile-actions .btn-primary {
        border: none;
        background: linear-gradient(135deg, var(--ac-blue), var(--ac-mid));
        box-shadow: 0 6px 16px rgba(28, 29, 171, .2);
    }
    .fo-profile-actions .btn-primary:hover { filter: brightness(.98); }

    .fo-profile-form .bo-filter-bar { margin-bottom: 0; }
    .fo-profile-form .form-label { font-size: .78rem; font-weight: 700; color: #6b7280; margin-bottom: .25rem; }
    .fo-profile-form .form-control,
    .fo-profile-form .form-select {
        border-radius: 9px;
        border: 1.5px solid #e5e7eb;
        background: #f9fafb;
        font-size: .88rem;
    }
    .fo-profile-form .form-control:focus,
    .fo-profile-form .form-select:focus {
        border-color: var(--ac-blue);
        box-shadow: 0 0 0 3px rgba(55,133,216,.1);
        background: #fff;
    }
    .fo-profile-form .form-text { font-size: .75rem; }
    .fo-profile-form .btn { border-radius: 10px; font-weight: 700; }

    @media (min-width: 992px) {
        .fo-profile-grid { grid-template-columns: 1fr 1fr; }
        .fo-profile-card-body { padding: 1.1rem; }
    }
</style>
